from ..src.model_ephemeris import ModelEphemeris

class TestModelEphemeris:
    """
    Tests:
        s initialization of object
        us initialization of object
        s method call of get_model_parameters (linear & quad)
        u method call of get_model_parameters (linear & quad)
        test adding kwargs
    """